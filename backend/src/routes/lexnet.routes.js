const router = require('express').Router()
// const ctrl = require('../controllers/lexnet.controller')
// const auth = require('../middleware/auth.middleware')

// TODO: definir rutas
// router.get('/', auth, ctrl.getAll)

module.exports = router
