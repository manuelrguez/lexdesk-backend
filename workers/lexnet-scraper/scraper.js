const { chromium } = require('playwright')

async function scrapeLexNet() {
  // TODO: implementar con certificado digital
  // const browser = await chromium.launch({ headless: true })
  console.log('📋 Scraper LexNet — pendiente de implementar con cert. digital')
}

module.exports = { scrapeLexNet }
